{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended"],
  "constraints": {
    "php": "8.2.0"
  },
  "constraintsFiltering": "strict",
  "pinDigests": true,
  "platformAutomerge": true,
  "minimumReleaseAge": "3 days",
  "prHourlyLimit": 0,
  "labels": ["renovate"],
  "description": [
    "constraints.php is the lowest PHP supported, not the newest; constraintsFiltering enforces it.",
    "No schedule: minimumReleaseAge is the only gate, so a window would just add latency on top of it."
  ],
  "packageRules": [
    {
      "description": "addLabels, not labels — labels is last-match-wins.",
      "matchManagers": ["composer"],
      "addLabels": ["composer"]
    },
    {
      "matchManagers": ["github-actions"],
      "addLabels": ["github-actions"]
    },
    {
      "description": "Pinned, so every release is a PR that CI fully verifies.",
      "matchManagers": ["composer"],
      "matchDepTypes": ["require-dev"],
      "rangeStrategy": "pin",
      "automerge": true,
      "addLabels": ["automerge"]
    },
    {
      "description": "Digest-pinned: a moved tag cannot slip in.",
      "matchManagers": ["github-actions"],
      "automerge": true,
      "addLabels": ["automerge"]
    },
    {
      "description": "The public contract — never widened without a manual OK from the Dependency Dashboard.",
      "matchManagers": ["composer"],
      "matchDepTypes": ["require"],
      "dependencyDashboardApproval": true,
      "rangeStrategy": "widen"
    },
    {
      "description": "Renovate reads its own constraints block as a dependency. Not an update.",
      "matchDepTypes": ["tool-constraint"],
      "enabled": false
    }
  ]
}
