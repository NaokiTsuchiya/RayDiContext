<?php

declare(strict_types=1);

// Stands in for a file whose survival a test asserts: an app file the cleaner must
// never touch, or a compiled script a rejected clean must leave in place.

return 0;
